{
  "name": "agent-manifest-diplomat",
  "version": "1.0.0",
  "private": true,
  "description": "Registration bridge between the Agent Manifest Ambassador and the public dataset.",
  "license": "MIT",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@agent-manifest/client": "^0.1.0"
  }
}
